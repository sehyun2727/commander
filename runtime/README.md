# runtime

Reserved for the future Commander Desktop local runtime wrapper (see `docs/ARCHITECTURE.md`
§ "Local Runtime"). In the MVP, the Dashboard runs directly in a browser against a localhost
API Server — this folder is not used yet.
