# config

Shared TypeScript, ESLint, and Prettier configuration presets, extended by `apps/dashboard`
and other JS/TS packages so tooling config isn't duplicated per package.

Status: skeleton only — no presets defined yet.
