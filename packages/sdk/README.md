# sdk

Typed client for the Commander API Server's REST/WebSocket endpoints. Consumed by
`apps/dashboard` today; intended as the shared entry point for any future client
(CLI, mobile) rather than each client re-implementing HTTP calls.

Status: skeleton only — no client code generated yet.
