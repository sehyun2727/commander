================================================
 COMMANDER — SPRINT PROGRESS
 Sprint: 13 — CEO Workspace Backend and Projection
 Overall: 67/95 (Phase 0-4 complete)
 Now working on: Phase 5 -- Regression, Audit, and Documentation
 Last update: 2026-08-18
================================================

PHASE 0 — Baseline and Read-Model Design             (13/14)
[x] 0.1 verify local HEAD == origin/master (f81cc7a, Sprint 12 close)
[x] 0.2 confirm working tree state (docs/prompts/sprint-13.md added by CEO;
        docs/prompts/sprint10.md -> sprint-10.md rename + sprint-12.md
        present, unrelated to Sprint 13)
[x] 0.3 read full sprint-13.md brief (742 lines) in full before starting
[x] 0.4 baseline backend suite: 297 passed / 4 skipped (matches Sprint 12 close)
[ ] 0.5 dashboard typecheck/build baseline run (deferred to Phase 4/5 --
        no frontend files touched yet in Phase 0)
[ ] 0.6 fresh seed/bootstrap re-verification (deferred to Phase 2/5 once
        the workspace module exists to bootstrap against)
[x] 0.7 inspected domain lifecycles/APIs/events/SSE: core/ownership.py,
        core/lifecycle/{task_states,agent_states,specification_states}.py,
        modules/approvals/service.py (+ workflow_engine.engine._mark_pending_approval
        for the real Approval.status value set), modules/timeline/routes.py,
        modules/event_bus/bus.py, modules/realtime/routes.py,
        modules/planning/service.py (ActiveSpecificationLockORM invariant),
        db_models.py (ProjectORM/AgentORM/RoleSingletonLockORM/TaskORM/
        SpecificationORM/SpecificationVersionORM), templates/software_company.py
        (RoleSpec.category/singleton, ROLES/ROLES_BY_KEY, PIPELINE),
        projects/routes.py + schemas.py (existing read-only Roles API)
[x] 0.8 identified authoritative source for every proposed workspace field
        (see DECISIONS.md #219 field map; dashboard query/hook architecture
        still to be confirmed directly in Phase 4, not blocking Phase 1-3)
[x] 0.9 confirmed event ordering key (EventORM.seq, autoincrement PK) and
        retention behavior (no pruning/retention limit exists anywhere)
[x] 0.10 cursor/incremental-update contract decided: reuse existing SSE +
        existing GET /api/projects/{id}/events cursor route; no new
        event-fetch endpoint or transport -- DECISIONS.md #217
[x] 0.11 next-action precedence mapped to real lifecycle predicates for
        all 10 tiers, including the tier-3 "no reachable predicate today"
        finding -- DECISIONS.md #218
[ ] 0.12 minimal frontend proof scope (deferred to Phase 4 design step)
[x] 0.13 N+1/indexing audit against situation/service.py + reports/service.py
        precedent (sequential independent selects, no joins, Python-side
        assembly) -- adopted for workspace/service.py, DECISIONS.md #219
[x] 0.14 PROGRESS.txt replaced with Sprint 13 live checklist (this file)
[x] 0.15 commit + push Phase 0 checkpoint (e72c657)

PHASE 1 — Public Schemas and Projection Policy        (11/11)
[x] 1.1 typed public workspace schemas (schema_version, summaries, focus,
        pending_actions, next_action, planning/mission summaries,
        recent_activity, event_cursor, generated_at) --
        app/modules/workspace_overview/schemas.py
[x] 1.2 explicit schema_version field (SCHEMA_VERSION = 1)
[x] 1.3 bounded summary models (recent missions/activity/pending/employees
        capped with documented limits -- MAX_RECENT_MISSIONS=10,
        MAX_ACTIVE_MISSIONS=10, MAX_RECENT_ACTIVITY=20, MAX_EMPLOYEES=25)
[x] 1.4 pending-action derivation (clarification/spec-review/approval/
        failure-setup) as pure functions over facts -- next_action.derive()
[x] 1.5 deterministic next-action policy (next_action.py, 10-tier order,
        pure dataclass-facts input, no DB access)
[x] 1.6 safe deep-link targets (route/resource-type/id per action kind --
        _spec_route/_mission_route/_decisions_route/_employees_route/
        _missions_route in next_action.py, matching real dashboard routes)
[x] 1.7 activity summary serialization (safe-field allowlist enforced --
        ActivityItem exposes only id/seq/type/kind/actor_role/actor_name/
        reason/created_at, never EventORM.payload; verified by
        test_workspace_schemas.py)
[x] 1.8 event cursor encoding/validation (reuse EventORM.seq int per
        DECISIONS.md #217 -- no new encoding; WorkspaceSnapshot.event_cursor
        is a plain int, malformed values rejected by Pydantic validation)
[x] 1.9 pure/domain tests: precedence (10 tiers), deterministic selection,
        no-action state, missing-target handling (focus/next_action never
        desync), safe serialization (forbidden-field-name audit across the
        whole schema tree), cursor round-trip, malformed cursor, stable
        ordering -- test_workspace_next_action.py (15) +
        test_workspace_schemas.py (5) = 20 tests, all passing
[x] 1.10 update PROGRESS.txt (this update)
[x] 1.11 commit/push Phase 1 (next action)

PHASE 2 — Projection Service and Workspace API        (12/14)
[x] 2.1 company-scoped projection service --
        app/modules/workspace_overview/service.py: get_workspace_snapshot()
[x] 2.2 load snapshot data through bounded, sequential queries (~11 selects
        in one session, no per-row queries)
[x] 2.3 avoid obvious N+1 patterns (verified: test_overview_service_issues_
        a_bounded_fixed_number_of_queries asserts <=15 SELECTs regardless
        of row count)
[x] 2.4 add indexes only if justified by an observed pattern -- no new
        index added; no N+1/slow-scan pattern observed at this data scale
[x] 2.5 produce coherent snapshot + cursor
[x] 2.6 add GET /api/projects/{project_id}/workspace/overview route
        (workspace_overview_router registered in main.py)
[x] 2.7 confirm existing SSE covers incremental activity (no new route
        needed per DECISIONS.md #217) -- documented explicitly in
        workspace_overview/routes.py module docstring
[x] 2.8 enforce auth + ownership (project_owned_by, 404 not 403) -- tested
        (unauthenticated 401, unknown company 404, cross-account 404)
[x] 2.9 structured errors: unknown company -> 404. No pagination/limit
        input surface exists on this endpoint (single project_id path
        param only -- documented in routes.py), so there is nothing to
        malform; every list in the snapshot is server-bounded, not
        client-paged.
[x] 2.10 invalid/expired cursor recovery semantics documented in
        routes.py -- no expiry possible (no retention pruning exists
        anywhere); reconnect/gap suspicion -> dashboard refetches this
        snapshot rather than replaying a partial event window
[x] 2.11 API/integration tests (test_workspace_overview_api.py, 15 tests):
        unauthenticated, unknown company, cross-company denial, fresh
        company (vacant leadership before start_mission), leadership
        filled (start_mission), clarification required, ready for review,
        approved/ready-to-execute, active mission, mission failure,
        pending approval, bounded recent missions/activity, no raw
        payload leakage, event_cursor == max(EventORM.seq)
[x] 2.12 inspect query count/SQL behavior -- explicit query-counting test
        via SQLAlchemy `before_cursor_execute` listener (see 2.3)
[x] 2.13 update PROGRESS.txt (this update)
[x] 2.14 commit/push Phase 2 (8ecac55)

PHASE 3 — Event and Lifecycle Integration             (12/12)
[x] 3.1 verify relevant domain transitions already emit events usable as
        safe workspace activity -- read app/core/events/types.py directly:
        full EventType coverage already exists for the entire specification
        lifecycle (REQUESTED/TURN_POSTED/CLARIFICATION_REQUESTED/ANSWERED/
        READY_FOR_REVIEW/APPROVED/REVISION_REQUESTED/REJECTED/CANCELLED/
        FAILED), task lifecycle, agent lifecycle, and approvals -- no new
        event types needed, confirmed by reading the enum rather than
        assuming a gap existed
[x] 3.2 verify specification/approval/revision/cancellation/mission/
        Employee transitions all surface correctly in the snapshot --
        test_workspace_overview_api.py (Phase 2, per-state) +
        test_workspace_lifecycle.py (Phase 3, transitions across a single
        live Company)
[x] 3.3 stable incremental ordering (seq-based, verified) -- documented in
        workspace_overview/routes.py; test_overview_event_cursor_matches_
        max_seq_for_the_project already proves event_cursor tracks
        EventORM.seq (autoincrement PK), not created_at, so equal
        timestamps never desync ordering
[x] 3.4 duplicate-safe consumption (client dedup by event.id, already the
        dashboard convention) -- documented in routes.py: this route never
        pushes events itself (no new transport per DECISIONS.md #217), so
        the existing SSE/event.id dedup convention (CLAUDE.md 9.4) is
        unchanged and out of scope for this endpoint
[x] 3.5 reconnect/retention-gap behavior defined (refetch snapshot) --
        already documented in routes.py from Phase 2 (item 2.10)
[x] 3.6 next_action changes after authoritative transitions (test) --
        test_workspace_lifecycle.py::
        test_next_action_tracks_the_full_founding_to_execution_lifecycle
        (setup_leadership -> start_mission -> answer_clarification ->
        review_specification -> begin_execution -> never begin_execution
        again once a Mission exists)
[x] 3.7 approval-to-execution readiness verified (tier 6 predicate test) --
        covered by the same lifecycle test (begin_execution fires exactly
        once, immediately after /approve, and never again after
        /begin-execution creates the Mission)
[x] 3.8 cancellation/failure removes stale actions (test) --
        test_workspace_lifecycle.py::
        test_next_action_clears_once_a_ready_for_review_specification_is_rejected,
        test_next_action_clears_once_planning_is_cancelled,
        test_next_action_clears_pending_approval_once_decided (this last
        one required reading approvals/service.py fresh: decide() takes
        (session_factory, workflow_engine, approval_id, decision, comment)
        and delegates the actual ApprovalORM.status mutation to
        workflow_engine.resume_after_decision -- decision is the literal
        "approve", not "approved"; discovered and fixed before running)
[x] 3.9 concurrency/integration properties: approval during snapshot, event
        fetch during transition, duplicate event delivery, equal
        timestamps, cross-company cursor, retention gap, stale target --
        verified by inspection and documented in routes.py rather than by
        new tests, because each is a direct consequence of a decision
        already tested elsewhere (single-session torn-read freedom from
        service.py's one-session-per-request design; ownership-gated
        cross-company access from test_overview_cross_account_access_
        returns_404_not_403; no caching/no stored snapshot means no
        stale-target state can exist at all)
[x] 3.10 run an API-level mock lifecycle, capture snapshot/action changes
        at each stage -- test_workspace_lifecycle.py::
        test_next_action_tracks_the_full_founding_to_execution_lifecycle
        asserts next_action.kind at every stage from a fresh Company
        through Mission creation
[x] 3.11 update PROGRESS.txt (this update)
[x] 3.12 commit/push Phase 3 (next action)

PHASE 4 — Minimal Dashboard Contract Proof            (16/16)
[x] 4.1 typed workspace client (lib/api.ts addition) -- api.getWorkspaceOverview,
        plus full WorkspaceSnapshot type tree in lib/types.ts mirroring
        schemas.py 1:1 (named to match the backend Pydantic model, distinct
        from WorkspaceFileEntry/WorkspaceMergeRecord which belong to the
        unrelated git Repository browser -- DECISIONS.md #220)
[x] 4.2 snapshot query (lib/hooks.ts addition) -- useWorkspaceOverview,
        30s refetchInterval fallback (same pattern as useSituation),
        keys.workspaceOverview added
[x] 4.3 incremental event/SSE integration (reuse RealtimeProvider) --
        keys.workspaceOverview added to invalidateForEvent() so any
        persisted event over the existing SSE connection refetches the
        snapshot; no new transport (DECISIONS.md #217)
[x] 4.4 reconnect/refetch fallback -- the 30s poll in 4.2 plus
        RealtimeProvider's existing EventSource auto-reconnect (browser
        native retry) cover the case where an SSE gap is missed entirely
[x] 4.5 minimal overview/proof surface -- new additive route
        app/company/[id]/overview/page.tsx, explicitly labelled "(Preview)"
        in its header and Sidebar link so it can't be confused with the
        final Sprint 14 CEO Workspace shell; does not touch the existing
        Headquarters page (app/company/[id]/page.tsx)
[x] 4.6 show next action + deep link -- renders next_action.title/
        explanation/urgency and a "Go there ->" Link to next_action.route
        verbatim from the server response
[x] 4.7 show concise pending/organization/mission/planning/activity
        summaries -- pending-action count, employee counts, active mission
        count, planning status, leadership roster, recent_activity feed
[x] 4.8 loading/empty/degraded/visible-error states -- isLoading text,
        ErrorState on isError, EmptyState for zero recent_activity
[x] 4.9 keep existing pages/navigation intact -- purely additive Sidebar
        entry; no existing route/component modified besides the
        invalidateForEvent() one-line addition (4.3)
[x] 4.10 avoid recreating next-action precedence in frontend code -- the
        page only renders next_action.kind/title/explanation/route as
        opaque server-provided strings; no urgency/kind switch statement
        chooses what to show next, only how to color an already-given
        urgency label
[x] 4.11 run typecheck -- `npm run typecheck` (tsc --noEmit): clean, no
        errors
[x] 4.12 run production build -- `npm run build`: succeeded,
        /company/[id]/overview listed in the route table (2.88 kB,
        113 kB First Load JS)
[x] 4.13 browser verification -- no browser-automation tool is available
        in this environment (checked via tool search); interactive
        click-through/visual verification is UNVERIFIED. What WAS verified
        instead, against real running dev servers (API on :8000 with
        COMMANDER_PROVIDER=mock, dashboard on :3000, Postgres already up
        via docker compose -- not the pytest harness): a live curl-driven
        walkthrough (register -> create company -> GET .../workspace/overview)
        returned a correct real snapshot (vacant CTO, auto-seeded PM/
        Reviewer/Engineer, founding activity, next_action=setup_leadership);
        curl against /company/{id}/overview returned HTTP 200 with no
        server-side crash/error markers, and correctly showed the
        RequireAuth client-side gate (expected for an unauthenticated curl
        request, since Commander auth is a session cookie the SPA checks
        client-side, not an SSR-blocking check)
[x] 4.14 (see 4.13) -- UNVERIFIED claim stated honestly, strongest actually-
        performed check (live curl walkthrough) reported instead of a false
        browser-verified claim, per CLAUDE.md §7.5/§16.7
[x] 4.15 update PROGRESS.txt (this update)
[x] 4.16 commit/push Phase 4 (next action)

PHASE 5 — Regression, Audit, and Documentation        (0/21)
[ ] 5.1 full backend tests
[ ] 5.2 dashboard typecheck
[ ] 5.3 dashboard production build
[ ] 5.4 verify migration from Sprint 12 (if any new migration added)
[ ] 5.5 verify fresh database bootstrap + seed
[ ] 5.6 run mock E2E with zero provider keys
[ ] 5.7 verify snapshot states across planning->review->approval->execution
[ ] 5.8 verify clarification and revision flows
[ ] 5.9 verify cancellation and failure cleanup
[ ] 5.10 verify auth and company isolation
[ ] 5.11 verify cursor resume, duplicate delivery, gap recovery
[ ] 5.12 audit response bounds and obvious N+1 behavior
[ ] 5.13 audit hidden reasoning/secrets/unsafe metadata
[ ] 5.14 audit generic-action/mutation leakage (no POST /workspace/action)
[ ] 5.15 audit frontend for duplicated lifecycle policy
[ ] 5.16 inspect complete diff for scope leakage
[ ] 5.17 update CLAUDE.md, PROGRESS.txt, README.md (if workflow changed),
        docs/ARCHITECTURE.md, docs/DECISIONS.md, docs/design/UX_SPEC.md
[ ] 5.18 record Sprint 14 boundaries and deferrals
[ ] 5.19 commit/push final documentation
[ ] 5.20 verify clean working tree
[ ] 5.21 verify local HEAD == origin/master

================================================
DESIGN DECISIONS RECORDED THIS SPRINT (docs/DECISIONS.md)
217. No new event transport/forward-cursor endpoint -- reuse SSE +
     existing GET /events?cursor= route.
218. next_action tier 3 has no reachable predicate in the current shipped
     lifecycle; kept as a documented no-op slot, not invented.
219. New workspace projection module (schemas.py, service.py, routes.py,
     next_action.py); sequential-select projection pattern from
     situation/reports precedent; ActiveSpecificationLockORM guarantees
     at most one non-terminal Specification per project.
220. Correction to #219: module lives at app/modules/workspace_overview/
     (not app/modules/workspace/) and the route is
     GET /api/projects/{project_id}/workspace/overview, because
     app/modules/workspace_manager/ already owns /workspace/{tree,file,
     merges} for the git-backed Repository browser -- avoids a name/route
     vocabulary collision between "Repository (Workspace)" and "Dashboard
     (CEO Workspace)" per CLAUDE.md's terminology table.
================================================
