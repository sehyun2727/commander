================================================
 COMMANDER — SPRINT PROGRESS
 Sprint: 15 — Safe CEO Workspace Widget System
 Overall: Phases 0-4 done, starting Phase 5.
 Now working on: Phase 5 — Regression, Security Audit, and Documentation
 Last update: 2026-08-18
================================================

PHASE 0 — Baseline and Architecture Decision           (9/9)
[x] 0.1 verify local HEAD == origin/master (45bcf11, Sprint 14 close);
        working tree clean except untracked docs/prompts/sprint-15.md;
        git fetch showed no divergence
[x] 0.2 baseline backend suite via apps/api/.venv (system `python` lacked
        asyncpg -- ModuleNotFoundError against app.core.db at collection
        time; the project venv has it): 336 passed / 4 skipped, matches
        the Sprint 14 close baseline exactly, zero regressions
[x] 0.3 dashboard typecheck (tsc --noEmit) and production build both clean
        on the Sprint 14 baseline (all 19 routes compile), before any
        Sprint 15 edits
[x] 0.4 read full sprint-15.md brief (838 lines) in full before starting
[x] 0.5 re-read Sprint 14's PROGRESS.txt handoff/low-confidence section:
        Widget system explicitly deferred to this sprint; browser-rendered
        interaction remains UNVERIFIED (no browser automation tool, #222) --
        re-confirmed via ToolSearch, same finding still holds
[x] 0.6 audited auth/ownership model: `get_current_user` (deps.py, session
        cookie only, never a client-supplied id) + `project_owned_by`
        (core/ownership.py, Rule #15 -- 404 not 403); `workspace_overview/
        routes.py` and `skill_templates/routes.py` both already compose
        these two exactly, no new primitive needed
[x] 0.7 audited existing settings/preferences patterns: `SettingORM` is a
        global KV store (not per-user/per-project, wrong shape for this);
        `SettingsPage.tsx` establishes the one existing explicit-save
        mutation-UX precedent (Save button, isPending/"Saved." states,
        window.confirm for destructive actions) -- reused for widget edit
        mode instead of inventing an autosave pattern; `skill_templates/
        registry.py` is the direct template for the new widget registry
        (frozen dataclass tuple + BY_KEY dict, zero per-project state)
[x] 0.8 audited frontend deps (package.json): no drag-and-drop/sortable
        library present -- confirms accessible move-up/move-down buttons
        over introducing a new DnD dependency (brief §4.9/§7.6)
[x] 0.9 architecture decisions recorded in docs/DECISIONS.md #228:
        persistence = one JSON column per (user, project) row, not
        normalized rows; concurrency = integer `revision` body field, not
        ETag/updated_at; registry = new `workspace_widgets` module mirroring
        `skill_templates`; 8-widget catalog mapped 1:1 onto Sprint 14's
        existing components/workspace/* (required: primary_next_action,
        connection_status); edit/save = explicit Edit mode + Save/Cancel,
        not autosave; responsive = one canonical order, half/full span
        packing, no free x/y grid

PHASE 1 — Registry, Persistence, and Validation         (14/14)
[x] 1.1 canonical registry (app/modules/workspace_widgets/registry.py):
        frozen WidgetDefinition dataclass, 8 widgets (primary_next_action +
        connection_status required/full, current_focus/pending_attention/
        planning_summary/missions_summary/organization_summary/
        recent_activity optional/half), WIDGETS tuple + WIDGETS_BY_KEY dict
        + REQUIRED_WIDGET_KEYS frozenset, dense unique default_order
[x] 1.2 typed public schemas (schemas.py): WidgetDefinitionResponse,
        WidgetPreferenceEntry (order>=0, span Literal["full","half"]),
        WorkspacePreferences (schema_version/revision/widgets bounded by
        MAX_WIDGET_ENTRIES/updated_at), WorkspacePreferencesUpdateRequest
[x] 1.3 preference persistence ORM model (WorkspacePreferenceORM in
        core/db_models.py, UniqueConstraint(user_id, project_id)) +
        Alembic migration 77037fd534fa (down_revision 7a1c9e3f2b6d)
[x] 1.4 default generation + normalization service (service.py:
        _default_widgets, _normalize -- tolerant, never raises, repairs
        stored/legacy rows on every GET)
[x] 1.5 validation (validate_update_entries -- strict, raises
        DuplicateWidgetError/UnknownWidgetError/MissingWidgetError/
        RequiredWidgetHiddenError on PUT; known keys, uniqueness, required-
        visible, span/order bounds all enforced via schemas + service)
[x] 1.6 optimistic-concurrency update path (integer `revision` body field;
        mismatch raises core.errors.StaleRevisionError -> 409 in routes.py)
[x] 1.7 tests: registry immutability / default layout
        (test_workspace_widgets_registry.py, 5 tests)
[x] 1.8 tests: malformed/unknown/duplicate entries normalize safely
        (test_malformed_stored_entries_normalize_safely)
[x] 1.9 tests: required-widget restoration
        (test_required_widget_hidden_in_storage_is_restored,
        test_update_rejects_required_widget_hidden)
[x] 1.10 tests: new/retired-widget normalization
        (test_stored_entries_missing_a_widget_get_it_added_back)
[x] 1.11 tests: stale-revision conflict
        (test_stale_revision_conflict_is_rejected)
[x] 1.12 tests: user/company uniqueness + concurrent first-creation
        (test_preferences_are_isolated_per_project_for_same_user,
        test_preferences_are_isolated_per_user_for_same_project,
        test_concurrent_first_creation_yields_exactly_one_row -- genuine
        asyncio.gather race, mirrors test_employee_singleton.py)
[x] 1.13 migration verification: upgrade head -> downgrade -1 -> upgrade
        head round-trip against live local Postgres, all clean
[x] 1.14 commit/push Phase 1 checkpoint: full backend suite re-run after
        all Phase 1 work (356 passed, 4 skipped -- 336 baseline + 20 new
        workspace_widgets tests, zero regressions); committed and pushed

PHASE 2 — Preferences API and Isolation                 (14/14)
[x] 2.1 widget-registry endpoint (GET .../workspace/widgets, built during
        Phase 1's routes.py pass, verified against Phase 2's requirements)
[x] 2.2 effective-preferences endpoint (GET .../workspace/preferences)
[x] 2.3 update endpoint (PUT .../workspace/preferences)
[x] 2.4 reset endpoint (POST .../workspace/preferences/reset)
[x] 2.5 auth required on all 4 routes (get_current_user, session cookie)
[x] 2.6 ownership enforced on all 4 routes (project_owned_by, Rule #15 --
        404 not 403, never a client-supplied user id)
[x] 2.7 GET responses are always the normalized, authoritative
        representation (service.get_effective_preferences normalizes on
        every read)
[x] 2.8 structured 4xx mapping in routes.py: Duplicate/Unknown/Missing ->
        400, RequiredWidgetHidden -> 422, oversized widgets list -> 422
        (enforced structurally by WorkspacePreferencesUpdateRequest's
        Field(max_length=MAX_WIDGET_ENTRIES))
[x] 2.9 StaleRevisionError -> 409 with {error, current_revision} body
[x] 2.10 observability stays presentation-only (Python logging only, no
        EventBus/Timeline emission -- DECISIONS.md #229)
[x] 2.11 API/integration tests (test_workspace_widgets_api.py, 17 tests
        over real HTTP via api_client/harness): unauthenticated 401 x2,
        cross-account 404, widgets catalog matches registry, default read,
        valid update + persists across reload, reset, per-company
        isolation, per-user isolation, unknown/duplicate/missing/
        required-hidden rejection, stale-revision 409, oversized-payload
        422, and a business-state-non-mutation check (event_cursor
        unchanged by preference writes)
[x] 2.12 live mock-mode API walkthrough via curl against a real uvicorn
        process + local Postgres: register -> create company -> GET
        widgets -> GET preferences (default, revision 1) -> PUT (revision
        bumps to 2) -> PUT with wrong widgets while stale (400, validated
        before revision check, as designed) -> POST reset (revision 3) ->
        unauthenticated GET (401) -- all behaved as specified
[x] 2.13 PROGRESS.txt updated per completed item (this entry)
[x] 2.14 commit/push Phase 2 checkpoint

PHASE 3 — Registry-Driven Workspace and Edit Mode        (19/19)
[x] 3.1 converted app/company/[id]/page.tsx from Sprint 14's hardcoded
        fixed sections to registry-driven composition (WorkspaceWidgetGrid)
[x] 3.2 reused every existing Sprint 14 widget component unchanged
        (ConnectionStatusBar, CurrentFocusCard, MissionSummaryCard,
        OrganizationSummaryCard, PendingAttentionList, PlanningSummaryCard,
        PrimaryActionPanel, RecentActivityList)
[x] 3.3 safe static component map added
        (components/workspace/widgetComponents.tsx --
        Record<string, ComponentType>, literal keys only, DECISIONS.md #230)
[x] 3.4 effective-preference queries/hooks added (lib/hooks.ts:
        useWorkspaceWidgets, useWorkspacePreferences,
        useUpdateWorkspacePreferences, useResetWorkspacePreferences,
        isStaleRevisionConflict; lib/api.ts: listWorkspaceWidgets/
        getWorkspacePreferences/updateWorkspacePreferences/
        resetWorkspacePreferences; lib/types.ts: WidgetDefinition/
        WidgetPreferenceEntry/WorkspacePreferences)
[x] 3.5 distinct edit mode added (components/workspace/WorkspaceEditMode.tsx
        -- full-page takeover, not a modal, entered/exited via an
        accessible "Customize Workspace"/"Close" button pair)
[x] 3.6 reorder controls (Move up/down buttons per visible widget, swap
        `order` within the visible subset only -- DECISIONS.md #230)
[x] 3.7 hide/restore controls (Hide button per non-required visible
        widget; Restore button per hidden-tray widget)
[x] 3.8 reset-to-default (window.confirm-gated immediate server call,
        same pattern as SettingsPage.handleArchive)
[x] 3.9 save/cancel (explicit, not autosave, per DECISIONS.md #228) with
        isPending "Saving…" state
[x] 3.10 dirty/saving/success/error states (dirty computed by structural
        entry comparison; Save disabled when !dirty; inline error banner
        on non-conflict failures; announcement region on save/reset)
[x] 3.11 stale-write conflict UX (409 -> amber conflict banner with an
        explicit "Reload Latest Layout" action; local edits preserved,
        never silently clobbered or auto-merged)
[x] 3.12 optimistic rollback: not applicable -- Phase 3 deliberately does
        NOT use optimistic UI (Save is request-then-commit-locally,
        matching the explicit-save decision in DECISIONS.md #228); no
        rollback needed because the UI is never updated ahead of the
        server's response
[x] 3.13 company-switch preference isolation (query keys keyed by
        companyId: keys.workspaceWidgets/workspacePreferences -- switching
        /company/[id] can never read another company's cached layout)
[x] 3.14 required-widget removal prevented in the UI (Hide button omitted
        entirely for entries where catalog.required is true, in addition
        to the existing server-side RequiredWidgetHiddenError backstop)
[x] 3.15 no dynamic imports anywhere in the widget system (verified:
        widgetComponents.tsx is a static literal object, no next/dynamic,
        no computed import path)
[x] 3.16 targeted tests supported by existing tooling: none -- confirmed
        (again, same as Sprint 14) no jest/vitest/testing-library/
        playwright config or dependency exists anywhere in apps/dashboard;
        typecheck + build are the only existing frontend verification
        tools, both run below
[x] 3.17 typecheck (tsc --noEmit) clean; production build clean, all 19
        routes compile (/company/[id] included, now 9.48 kB)
[x] 3.18 PROGRESS.txt updated per completed item (this entry)
[x] 3.19 commit/push Phase 3 checkpoint

PHASE 4 — Failure Isolation, Responsive, and Accessibility (21/21)
[x] 4.1 per-widget error isolation: new
        components/workspace/WidgetErrorBoundary.tsx (this codebase's first
        class component -- React error boundaries require
        componentDidCatch/getDerivedStateFromError, no hook equivalent
        exists); WorkspaceWidgetGrid wraps each rendered widget
        individually so one widget throwing shows a small isolated
        "couldn't load" card + Retry, never unmounts the rest of the
        Workspace; no stack trace/payload ever rendered, error only
        goes to console.error
[x] 4.2 stronger next-action fallback: WidgetErrorBoundary's `critical`
        prop (set only for primary_next_action in WorkspaceWidgetGrid)
        renders a full-width amber card with plain-language copy + a
        Refresh button instead of the generic small red card, so the
        CEO's main action degrades safely rather than silently
        vanishing (brief §4.11)
[x] 4.3 unsupported-widget fallback formalized: normal mode already
        omitted unsupported keys safely with a deduped console.warn
        (Phase 3); edit mode already showed an inline "Unsupported --
        will not display" badge (Phase 3) -- both re-verified against
        §7.1's exact requirement list, no gaps found
[x] 4.4 focus preservation after reorder: boundary-move case (Move
        up at position 1 / Move down at the end disables the button
        just pressed) previously dropped focus to <body>; added
        `focusRequest` state + `buttonRefs` map in WorkspaceEditMode so
        the still-enabled sibling button (Move down/up) on the same
        widget is focused after the reordered list re-renders
[x] 4.5 screen-reader position announcements: aria-live="polite"
        region (already added Phase 3) announces
        "<title> moved to position X of Y" on every reorder, plus
        hide/restore/save/reset outcomes
[x] 4.6 keyboard-only reorder/hide/restore/reset verified by code
        inspection: every control is a native <button type="button">
        with an explicit aria-label (no click-only <div>s anywhere in
        the widget system), so Tab+Enter/Space operates all of them by
        default; no drag-and-drop exists, so no keyboard alternative is
        needed beyond the buttons themselves
[x] 4.7 mobile edit mode verified by code inspection: EditModeShell
        and both widget lists use flex-col on mobile / sm:flex-row on
        desktop, same responsive pattern as the rest of the dashboard
        (SettingsPage, etc.)
[x] 4.8 touch-sized controls: Move up/down, Hide, and Restore buttons
        get min-h-[44px] min-w-[44px] (WCAG 2.5.5) below the `sm`
        breakpoint, reverting to their original compact size at sm:
        and above -- desktop Edit Mode is control-dense and this
        codebase's existing buttons are already well under 44px there,
        so only the touch-input case needed the fix (DECISIONS.md #231)
[x] 4.9 desktop and mobile ordering verified by code inspection: both
        derive from the same sorted `visible` list (WorkspaceWidgetGrid
        packRows for desktop pairing, mobile just stacks it) -- no
        separate mobile order exists to drift out of sync
[x] 4.10 company switching verified by code inspection: keys.
        workspaceWidgets(companyId)/workspacePreferences(companyId) are
        keyed by companyId (lib/hooks.ts), so navigating to a different
        /company/[id] always issues a fresh query under a new key
[x] 4.11 no cross-company layout/data flash verified by code
        inspection: none of the workspace queries use
        placeholderData/keepPreviousData, so a company switch shows
        WorkspaceSkeleton (isLoading) rather than the previous
        company's stale widgets momentarily
[x] 4.12 navigation with unsaved changes verified by code inspection:
        beforeunload guard (tab close/reload) active only while dirty,
        window.confirm on Cancel/Close discards intentionally -- no
        in-app router-level guard exists anywhere in this codebase to
        extend (DECISIONS.md #230's documented scope boundary)
[x] 4.13 reset confirmation verified by code inspection:
        handleReset requires window.confirm before calling the reset
        mutation, matching SettingsPage.handleArchive's precedent
[x] 4.14 loading/empty/error/conflict states verified by code
        inspection: WorkspaceSkeleton (page.tsx) / EditModeShell's
        pulse skeleton, ErrorState on isError, "Every optional widget
        is currently visible" empty state for the hidden tray, amber
        conflict banner + red error banner in WorkspaceEditMode -- all
        four states have distinct, non-overlapping UI
[x] 4.15 typecheck (tsc --noEmit) clean after all Phase 4 edits
[x] 4.16 production build clean after all Phase 4 edits, all 19 routes
        compile (/company/[id] now 10 kB)
[x] 4.17 targeted tests supported by existing tooling: none -- same
        finding as Phase 3, still holds (no jest/vitest/
        testing-library/playwright in this repo)
[x] 4.18 browser interaction verification: re-confirmed via ToolSearch
        that no browser automation tool exists in this environment
        (same finding as Phase 0's audit)
[x] 4.19 interactive drag/click/touch/visual checks recorded as
        UNVERIFIED per CLAUDE.md §16.7 -- everything in this phase
        that requires literal on-screen/on-device observation (focus
        ring visibility, actual tap comfort, real screen-reader output)
        is NOT claimed as verified; only code-level guarantees
        (semantics, ARIA attributes, query-key isolation, CSS classes)
        are claimed, and are listed as such above
[x] 4.20 PROGRESS.txt updated per completed item (this entry)
[x] 4.21 commit/push Phase 4 checkpoint

PHASE 5 — Regression, Security Audit, and Documentation  (0/24)
