================================================
 COMMANDER — SPRINT PROGRESS
 Sprint: 14 — CEO Workspace UI Shell
 Overall: Phase 0-3 done (Phase 4-5 remaining)
 Now working on: Phase 4 — Responsive, accessibility, workflow polish
 Last update: 2026-08-18
================================================

PHASE 0 — Baseline and UX Audit                       (12/12)
[x] 0.1 verify local HEAD == origin/master (88196f2, Sprint 13 close);
        working tree clean except untracked docs/prompts/sprint-14.md
[x] 0.2 baseline backend suite: 336 passed / 4 skipped (matches Sprint 13
        close, no regressions)
[x] 0.3 dashboard typecheck (tsc --noEmit) and production build both clean
        on the Sprint 13 baseline, before any Sprint 14 edits
[x] 0.4 read full sprint-14.md brief (767 lines) in full before starting
[x] 0.5 confirmed no browser-automation tool exists in this environment
        (ToolSearch, re-checked) -- browser/click-through verification will
        be recorded UNVERIFIED per §4.10, same finding as Sprint 13 (#222)
[x] 0.6 audited current shell/routes/components: app/company/[id]/layout.tsx
        (fixed w-60 Sidebar, zero responsive handling -- primary gap),
        app/company/[id]/page.tsx (Headquarters -- DecisionCard,
        SituationReport, StatCard, TimelineFeed), overview/page.tsx (Sprint
        13 proof page), RealtimeProvider.tsx (found company-switch state
        bug, see DECISIONS.md #224), useEventStream.ts (3-state
        ConnectionStatus, no stale/offline distinction, see #225),
        Sidebar.tsx, AccountBadge.tsx, StatusPill/StatusWord.tsx,
        AgentAvatar.tsx, DecisionCard.tsx, MissionCard.tsx,
        SpecificationCard.tsx, SpecificationStatusBadge.tsx,
        TimelineFeed.tsx, lib/auth-context.tsx, lib/utils.ts, lib/hooks.ts
        (full), lib/types.ts (WorkspaceSnapshot family, L279-405),
        tailwind.config.ts (no custom breakpoints, fixed dark theme), no
        frontend test framework exists (confirmed, none added)
[x] 0.7 identified /company/[id]/workspace as a PRE-EXISTING, unrelated
        route (git-backed Repository browser, workspace_manager module) --
        not to be touched; confirmed via DECISIONS.md #220 this collision
        was already anticipated at the API level
[x] 0.8 mapped every next_action.kind (next_action.py, 10 reachable + 1
        reserved-unreachable tier) to its real route: all already resolve
        to existing pages (specifications/[id], missions/[id], decisions,
        employees, missions) via the module's own _spec_route/
        _mission_route/_decisions_route/_employees_route/_missions_route
        builders -- no unmapped/dead target found
[x] 0.9 pending_actions item schemas carry no `route` field (only IDs) --
        frontend must build their deep links itself from fixed templates
        mirroring next_action.py's route builders; decided display order
        mirrors next_action.py's own tier comment (DECISIONS.md #226)
[x] 0.10 desktop/mobile information architecture decided: single DOM tree,
        two composition blocks gated by `hidden lg:grid` / `lg:hidden`
        (mobile = linear stack per brief §5, desktop = grouped 2-col rows)
        rather than fighting CSS `order` across a shared grid
[x] 0.11 query/subscription lifecycle: reuse useWorkspaceOverview (30s
        poll) + invalidateForEvent's existing workspaceOverview(companyId)
        entry as-is; add RealtimeProvider invalidation on
        reconnecting/stale -> open transition (§4.7); company switch fixed
        via RealtimeProvider key={companyId} remount (§4.8, #224)
[x] 0.12 recorded DECISIONS.md #223-226 (landing-route replacement instead
        of redirect-first staging, RealtimeProvider remount fix,
        ConnectionStatus stale/heartbeat tracking, pending-action route
        construction); this Phase 0 checkpoint commit

PHASE 1 — Responsive Shell and Navigation             (4/4)
[x] 1.1 Sidebar.tsx: mobile drawer (fixed+translate, backdrop, close-on-nav)
        + desktop static, rename root nav label "Headquarters" -> "CEO
        Workspace", drop "Overview (Preview)" entry
[x] 1.2 new MobileHeader.tsx: compact header, hamburger, AccountBadge
[x] 1.3 layout.tsx: wire mobile header + drawer state, RealtimeProvider
        key={companyId}, active-route indication preserved
[x] 1.4 typecheck/build checkpoint (clean, see Phase 2's combined run)

PHASE 2 — Core Workspace Sections                     (7/7)
[x] 2.1 lib/utils.ts: next-action kind tone map, safe-route allowlist,
        pending-action route builders, connection-status label helpers
[x] 2.2 useEventStream.ts: stale ConnectionStatus + lastMessageAt
[x] 2.3 RealtimeProvider.tsx: expose lastMessageAt, reconnect-triggered
        workspace invalidation
[x] 2.4 components/workspace/*: PrimaryActionPanel, CurrentFocusCard,
        PendingAttentionList, PlanningSummaryCard, MissionSummaryCard,
        OrganizationSummaryCard, RecentActivityList, ConnectionStatusBar
[x] 2.5 app/company/[id]/page.tsx: real Workspace composition
[x] 2.6 app/company/[id]/overview/page.tsx: redirect() to landing route
[x] 2.7 typecheck/build checkpoint: `tsc --noEmit` clean, `next build`
        clean (all routes compile, including untouched
        /company/[id]/workspace repo-browser route unaffected)

PHASE 3 — Live Updates and State Integrity            (3/3)
[x] 3.1 SSE-driven refetch + 30s poll fallback: live-verified SSE frame
        delivery (commander-event/heartbeat) against a running API+DB;
        confirmed by code that every persisted event still runs through
        the pre-existing invalidateForEvent(qc, companyId, ...) which
        includes keys.workspaceOverview(companyId) (Sprint 13, reused
        as-is), plus new reconnect/stale->open transition triggers a
        forced invalidateQueries on the same key (RealtimeProvider.tsx
        L88-95) so a connection that may have missed events is never
        silently stale; useWorkspaceOverview's 30s poll (lib/hooks.ts,
        pre-existing) is an independent fallback path, unchanged. Full
        client-side "UI actually updates on screen" behavior is
        UNVERIFIED (no browser automation tool in this environment,
        #222) -- this is a code-level + wire-level verification, not a
        rendered-DOM observation.
[x] 3.2 company-switch stale-flash mitigation: RealtimeProvider is now
        mounted with key={companyId} in layout.tsx, so React fully
        unmounts/remounts the SSE connection, event buffer, and
        streaming-reply state on every company switch instead of
        reusing stale state (DECISIONS.md #224). Visual "no flash"
        confirmation itself is UNVERIFIED (no browser automation) --
        the remount mechanism is the mitigation on record, not an
        observed screenshot.
[x] 3.3 loading/empty/error states per §6, live-checked against a real
        API+DB (not just reading component code):
        - error: GET /api/projects/{unknown-uuid}/workspace/overview ->
          404 {"detail":"Company not found"} (Rule #15 -- not 403);
          page.tsx's isError branch renders ErrorState on any non-2xx,
          confirmed by reading the query's error path (Sprint 13 hook)
        - empty: created a brand-new company via POST /api/projects
          (founding roster only, zero missions/specs/activity beyond
          founding events, CTO vacant) and fetched its live
          /workspace/overview: missions.active=[], planning.active=
          false, pending_actions all null, focus={role,cto,vacant},
          next_action=setup_leadership/"Hire a CTO". Cross-checked
          every affected component's empty-state branch: MissionSummary
          Card ("No active missions."), PlanningSummaryCard ("No active
          planning."), PendingAttentionList ("Nothing pending."),
          CurrentFocusCard/OrganizationSummaryCard (vacant CTO shown in
          amber, "View ->" routes to /employees), PrimaryActionPanel
          (known kind + safe route -> real CTA link, not the generic
          fallback). curl'd the page shell itself (200, no server
          exception) and confirmed dashboard dev log shows a clean
          compile/200 for this company id with no runtime errors.
        - loading: WorkspaceSkeleton (pulse blocks, no premature empty-
          state flash) verified by reading page.tsx's isLoading branch;
          not independently exercisable without throttling network in a
          real browser (UNVERIFIED per #222, same reasoning as 3.1/3.2)

PHASE 4 — Responsive, Accessibility, Polish           (0/?)
[ ] 4.1 mobile/desktop pass, keyboard/aria audit
[ ] 4.2 UNVERIFIED browser interaction note

PHASE 5 — Regression, Audit, Documentation            (0/?)
[ ] 5.1 full backend + frontend test/typecheck/build regression
[ ] 5.2 CLAUDE.md / ARCHITECTURE.md / UX_SPEC.md sync
[ ] 5.3 final commit + push + remote HEAD confirmation
