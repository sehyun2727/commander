================================================
 COMMANDER — SPRINT PROGRESS
 Sprint: 19 — OpenRouter provider, correlation ID / structured logging,
 load smoke, real-LLM E2E, deployment docs, V1.1 release readiness
 Overall: Sprint 19 complete (Phases 0-4 done). See CLOSE-OUT NOTES at
 the end of this file. The v1.1.0 tag is the CEO's own call, not part
 of this sprint's close-out.
 Now working on: nothing -- Sprint 19 closed out; awaiting CEO
 hands-on verification and the CEO's own v1.1.0 tag decision.
 Last update: 2026-08-19
================================================

(Sprint 18 close-out record retained below for history.)

PHASE 0 — Baseline, Repository Inspection, and Architecture Decisions
[x] 0.1 verify local HEAD == origin/master (5017b13, Sprint 17 close);
        working tree clean except untracked docs/prompts/sprint-18.md;
        Alembic head b1f4c8d5e9a2 -- matches sprint-18.md's stated
        baseline exactly
[x] 0.2 re-ran full backend suite: 472 passed / 6 skipped -- unchanged
        from Sprint 17 close-out, matches the CEO's stated baseline
[x] 0.3 re-ran dashboard tsc --noEmit + next build: clean, all 19 routes
        compiling -- unchanged
[x] 0.4 read full sprint-18.md brief (681 lines) in full before starting
[x] 0.5 read CLAUDE.md, docs/ARCHITECTURE.md §5 (Project Memory stub) +
        §6.1 (module table), docs/DECISIONS.md #233-242, FOR_CTO.md §5
        (Domain Model), §6 (Planning), §8 (Event Architecture), §9
        (Persistence), docs/design/UX_SPEC.md (confirmed zero mentions
        of Memory -- no UI expected, matches brief §8)
[x] 0.6 read every file named in sprint-18.md §3 required inspection:
        core/events/types.py, contracts.py, base.py, event_bus/bus.py,
        planning/orchestrator.py (full), planning/service.py (partial --
        SPECIFICATION_APPROVED publish site), templates/software_company.py
        (PM/CTO planning contracts + RoleSpec), main.py::lifespan,
        core/db_models.py (full table inventory + ORM conventions),
        alembic/versions/b1f4c8d5e9a2_harness_tool_calls.py (migration
        shape reference), core/ownership.py, tasks/service.py
        (get_harness_summary), agent_harness/output.py (bound_output,
        redact_environment_like_content), approvals/service.py + schemas.py
        (ApprovalORM shape), workflow_engine/engine.py (_fail_task,
        _fail_task_with_reason_code, _finish_task, _mark_pending_approval,
        REVIEW_COMPLETED/APPROVAL_* publish sites), test_planning_orchestrator.py
        + test_event_bus.py (test names/shape only)
[x] 0.7 inventoried every EventType currently emitted; confirmed all six
        §4.1 source event types exist: APPROVAL_GRANTED/REJECTED/
        CHANGES_REQUESTED, SPECIFICATION_APPROVED, REVIEW_COMPLETED,
        TASK_FAILED, TASK_COMPLETED, SPECIFICATION_TURN_POSTED; confirmed
        CONVERSATION_MESSAGE/_DELTA are never in this list (Rule #8)
[x] 0.8 inspected event_bus.subscribe(...) usage pattern -- confirmed
        InProcessEventBus.publish() runs subscribers inline, log-and-
        swallow on exception (bus.py:60-63); wiring point is
        main.py::lifespan right after `event_bus = InProcessEventBus(...)`
[x] 0.9 found and resolved an ordering hazard: REVIEW_COMPLETED
        publishes BEFORE the ApprovalORM row (with sections/raw_summary)
        is committed in engine.py -- a reviewer_feedback extractor that
        queried ApprovalORM by task_id inside the subscriber would race
        the write. Resolved by adding an additive
        ReviewCompletedPayload.sections field instead of reordering
        pipeline code -- see docs/DECISIONS.md #243. Every other
        projected event type was checked and is race-free (approval/
        spec-version/task rows are already committed before their event
        publishes).
[x] 0.10 decided tokenizer/stopword list: lower().split(), alphanumeric
        filter, dedupe, fixed stopword list -- docs/DECISIONS.md #243
[x] 0.11 decided recency-decay formula: 1/(1+age_days/30), not exp() --
        docs/DECISIONS.md #243
[x] 0.12 decided MAX_RECALL_* constants: brief's defaults adopted as-is
[x] 0.13 decided MemoryRecordORM shape + migration shape (new table only,
        down_revision=b1f4c8d5e9a2) -- docs/DECISIONS.md #243
[x] 0.14 confirmed projected event list matches brief §4.1 exactly
[x] 0.15 PROGRESS.txt replaced with this Sprint 18 checklist
[x] 0.16 decisions recorded in docs/DECISIONS.md #243
[x] 0.17 Phase 0 checkpoint commit + push -- along with it, implemented
        the two Phase-0-adjacent additive contract changes the design
        decisions required (MEMORY_RECORDED/MEMORY_RECALLED EventTypes +
        payloads, ReviewCompletedPayload.sections, engine.py publish-site
        wiring) since they are pure contract/plumbing with zero behavior
        yet -- regenerated TS schemas (+20 lines, additive-only),
        confirmed dashboard tsc --noEmit still clean, confirmed targeted
        backend tests (review/event_contract/event_bus, 26 tests) still
        green

PHASE 1 — memory_records table, ORM, migration, projection module, subscriber wiring
[x] 1.1 core/events/types.py + contracts.py: MEMORY_RECORDED, MEMORY_RECALLED
        event types + payload contracts (bounded, no content bodies) --
        done as part of the Phase 0 commit (b844bd4), since it was pure
        contract/plumbing with zero behavior yet (see 0.17)
[x] 1.2 core/events/contracts.py: ReviewCompletedPayload.sections additive field;
        engine.py publish site populates it from the already-in-scope
        `sections` dict -- also done in b844bd4 (see 0.9, 0.17)
[x] 1.3 core/db_models.py: MemoryRecordORM
[x] 1.4 alembic migration: memory_records table + indexes
        (c2a7e1f4b6d3_memory_records.py, down_revision=b1f4c8d5e9a2) --
        verified against real Postgres: upgrade head, downgrade -1,
        re-upgrade head all clean; `alembic history` confirms a single
        linear chain with c2a7e1f4b6d3 on top
[x] 1.5 app/modules/memory/registry.py: six category strings, tag rules,
        tokenizer, bounding/recall constants, recency_decay()
[x] 1.6 app/modules/memory/schemas.py: MemoryRecord/RecallRequest/RecalledMemory
        (lenient field_validators per DECISIONS.md #244's
        malformed-tags-parse-as-empty-list requirement)
[x] 1.7 app/modules/memory/projection.py: one extractor per projected
        EventType -- async (event, session) per DECISIONS.md #244, not a
        pure payload-only function, since most payloads carry only ids
[x] 1.8 app/modules/memory/service.py: record_memory() (extract, insert,
        IntegrityError-catch dedup, MEMORY_RECORDED publish) -- recall()
        also implemented ahead of schedule (ranking/caps per §4.9-§4.11),
        wired up properly in Phase 2
[x] 1.9 app/modules/memory/subscriber.py: install_memory_subscribers()
[x] 1.10 main.py::lifespan wiring -- install_memory_subscribers(event_bus,
        session_factory) called immediately after `event_bus` construction,
        before agent_runtime/orphan recovery
[x] 1.11 unit tests (tests/test_memory_projection.py, 20 tests, all
        green): one extractor test per category (including reason_code
        variants + redaction + oversized-content truncation), extractor
        safety (malformed/null/missing-row payloads return None and never
        insert), unprojected-event-type short-circuit, UNIQUE dedup
        (record_memory called twice -> one row, same id returned),
        subscriber wiring through a real EventBus.publish, double-install
        idempotency, and a raising extractor proven not to break publish
        fan-out to other subscribers
[x] 1.12 full backend suite re-run after Phase 1: 492 passed / 6 skipped
        (472 baseline + 20 new memory tests, 0 regressions, exit 0,
        437.94s) -- Phase 1 commit + push next

PHASE 2 — recall service + PM contract extension + planning integration
[x] 2.1 app/modules/memory/service.py: recall() with deterministic ranking
        -- implemented ahead of schedule in Phase 1; this session fixed a
        real bug it shipped with (naive-vs-aware datetime subtraction blew
        up under SQLite, which -- unlike Postgres -- hands back a tz-naive
        `created_at` even though the column is DateTime(timezone=True));
        normalized with the same tzinfo-is-None guard workflow_engine/
        engine.py already uses for its budget-elapsed check, in both the
        per-row age_days computation and the tie-break sort key
[x] 2.2 MAX_RECALL_* caps enforced server-side (silent truncation) --
        verified via test_memory_recall.py::test_recall_limit_is_capped_server_side
        (limit=1000 request -> exactly MAX_RECALL_LIMIT=10 results)
[x] 2.3 templates/software_company.py: _PM_PLANNING_CONTRACT gains
        recall_request field description (PM turn kinds only, not CTO) --
        documents the JSON shape, that all sub-fields are optional, and the
        categories=null (search all six) vs categories=[] (search none)
        distinction per §4.11; _CTO_PLANNING_CONTRACT untouched
[x] 2.4 planning/orchestrator.py: parse optional recall_request from PM
        turn output, call memory.service.recall(), inject bounded
        message before next gateway.complete() call, publish MEMORY_RECALLED
        -- new `_maybe_recall()` method called from `_run`'s while loop
        right after `_persist_turn` whenever `role_key ==
        TEMPLATE.planning_pm_role_key`; the formatted message is threaded
        through a `pending_recall_message` local (this orchestrator has no
        persisted cross-turn message list -- every turn is one fresh
        one-shot `gateway.complete` call) into `_run_turn`/`_call_turn`,
        which prepend it as an extra user-role message ahead of the turn's
        normal one, consumed exactly once regardless of whether the next
        turn is PM or CTO
[x] 2.5 CTO turn kinds explicitly reject a present recall_request field
        via the existing malformed-attempt retry path -- new
        `_reject_recall_request()` called from `_validate_cto_review`/
        `_validate_cto_followup_answer`; new
        `_validate_recall_request_optional()` (structural-type check only,
        never raises on a malformed *sub-field* -- that is RecallRequest's
        own lenient-coercion job) called from every PM validator
[x] 2.6 tests: recall_request absent (unchanged Sprint 12 behavior),
        recall_request present + matches, empty categories, malformed
        types, CTO-turn-with-recall_request rejected -- 11 new recall()
        unit tests (tests/test_memory_recall.py: empty memory, tag/keyword
        ranking, recency-only ranking, tie-break on created_at then id,
        MAX_RECALL_LIMIT enforcement, categories=None vs [] vs explicit
        set, since_days cutoff, malformed sub-field coercion, project
        scoping) + 4 new planning-integration tests appended to
        tests/test_planning_orchestrator.py (recall_request absent stays
        silent, present-with-matches injects a message into the very next
        turn and publishes memory.recalled, zero-matches still publishes
        the event but injects no message, CTO turn carrying recall_request
        fails validation and the Specification ends FAILED/
        malformed_provider_output) -- all via the existing
        monkeypatch(MockProvider.complete) pattern, since the mock
        provider's deterministic fixture text has no recall_request
        scenario until Phase 3's demo marker
[x] 2.7 full backend suite re-run after Phase 2: 507 passed / 6 skipped
        (baseline 492 + 11 new test_memory_recall.py + 4 new
        test_planning_orchestrator.py recall-integration tests), zero
        regressions -- Phase 2 commit + push next

PHASE 3 — backfill + Sprint 17 signal projection + mock recall scenario
[x] 3.1 app/modules/memory/backfill.py: backfill_memory(session_factory,
        event_bus, *, project_id=None) -- selects EventORM rows whose type
        is in registry.PROJECTED_EVENT_TYPES (optionally scoped to one
        project), replays each through the same record_memory() path the
        live subscriber uses. Idempotent by construction (record_memory's
        own UNIQUE(source_event_id) IntegrityError handling), returns the
        count of events considered (not rows inserted)
[x] 3.2 scripts/backfill_memory.py: argparse CLI wrapper (--project-id,
        default = all companies), mirrors scripts/seed.py's sys.path/cwd
        setup so settings.database_url resolves the same dev DB path;
        `--help` verified
[x] 3.3 confirmed failed_attempts extractor already projects
        TASK_FAILED.reason_code faithfully -- traced Sprint 17's
        _fail_task_with_reason_code() (workflow_engine/engine.py) emitting
        payload={"task_id":..., "reason_code": "self_correction_exhausted"
        | "employee_surrendered"}, reason=<surrender/exhaustion text>,
        matches extract_from_task_failed() exactly; already covered by
        Phase 1's test_task_failed_with_self_correction_reason_code,
        test_task_failed_with_employee_surrender_preserves_bounded_preview,
        test_task_failed_redacts_environment_like_surrender_text -- no gap,
        no new test needed
[x] 3.4 mock_provider.py: RECALL_DEMO_MARKER = "RECALL_DEMO" (same
        fixture-marker convention as NEEDS_CLARIFICATION_MARKER etc.,
        gated by `not already_resumed`) added to _pm_analysis_text() --
        embeds a recall_request (categories=[failed_attempts],
        tags=[auth], keywords=[login]) in the PM's first analysis turn
        JSON when the CEO's request_text contains the marker
[x] 3.5 tests/test_planning_orchestrator.py::
        test_recall_demo_marker_runs_recall_end_to_end_via_real_mock_provider
        -- unlike the Phase 2 tests (which fully replace
        MockProvider.complete), this wraps the *real* complete() to only
        observe calls, driving the actual RECALL_DEMO_MARKER fixture path;
        asserts cto_review receives 2 messages incl. "Memory recall
        results" + the planted record's title, pm_draft_or_followup
        receives 1 (consumed once), and memory.recalled fires with
        match_count=1. Also added tests/test_memory_backfill.py (4 tests:
        projects every pre-existing projected event + skips unprojected
        types, idempotent on re-run, project_id=None covers all
        companies, project_id scopes to one company) -- all pass against
        the harness's own event_bus, which never has the memory
        subscriber installed (exactly the "Company predates the
        subscriber" scenario backfill targets)
[x] 3.6 scripts/generate_ts_schemas.py rerun -- diff was NOT purely
        additive: packages/event-schemas/ts/index.ts's MemoryRecalledPayload
        had gone stale since Phase 0 (TS said `specification_id: string`
        required + `requested_categories?: string[] | null`; Python's
        actual contracts.py MemoryRecalledPayload, matching what
        orchestrator.py publishes, is `spec_id: str | None = None` +
        `requested_categories: list[str]` non-null-always-resolved per
        sprint-18.md §4.12). Regeneration corrected the drift to match the
        Python source of truth exactly; no dashboard code referenced
        either field, confirmed via grep -- zero blast radius
[x] 3.7 dashboard tsc --noEmit clean, dashboard `next build` clean (19
        routes, unaffected by the TS payload correction), full backend
        suite: 512 passed / 6 skipped (507 Phase-2 baseline + 4 new
        test_memory_backfill.py + 1 new recall-demo-marker test), zero
        regressions -- Phase 3 commit + push next

PHASE 4 — Regression, security audit, documentation, close-out
[x] 4.1 full backend suite green: 512 passed / 6 skipped (472 baseline +
        40 Sprint 18 new/changed), zero regressions outside Sprint 18 files
[x] 4.2 dashboard tsc --noEmit + next build clean, all 19 routes -- no UI
        changes beyond the regenerated TS event contract
        (MemoryRecalledPayload drift fix, zero dashboard references)
[x] 4.3 migration chain verified via alembic heads/history + a real
        upgrade/downgrade/upgrade round-trip against a throwaway Postgres
        DB (commander_migration_check, created/dropped via docker exec) --
        deliberately not the real dev DB, to avoid risking in-progress
        dev-DB state on a destructive-adjacent operation
[x] 4.4 mock E2E with zero provider keys covering the new recall scenario
        (RECALL_DEMO_MARKER, test_recall_demo_marker_runs_recall_end_to_end_via_real_mock_provider)
        -- green
[x] 4.5 existing planning / specification / mission / workspace / widget /
        harness / self-correction regressions -- all green as part of the
        full 512-passed suite run in 4.1
[x] 4.6 independent security audit (dedicated read-only Explore agent, not
        self-audit) against the brief's 12-item checklist: 12/12 PASS with
        file:line evidence (extractor purity, zero LLM calls in
        projection/recall, project_id scoping enforced in recall(),
        DB-level UNIQUE(source_event_id), bounded/redacted excerpts,
        minimal event payloads, no public HTTP endpoint, subscriber
        exception isolation, server-enforced caps unconditional, CTO turns
        structurally rejected from recall, no Rule #16 violations)
[x] 4.7 full diff scope-leakage review (git diff --stat against the true
        pre-Sprint-18 baseline 5017b13, not a stray intermediate commit):
        26 files, 3394 insertions/122 deletions, zero dashboard changes,
        zero new HTTP routes, zero RAG/vector/cross-Company/Employee-tool
        scope leakage
[x] 4.8 documentation sync: CLAUDE.md roadmap row 18 -> "18 (checkmark)";
        docs/ARCHITECTURE.md §5 rewritten to as-built Memory + §6.1 module
        table `memory` row + §4.2 cross-reference; docs/DECISIONS.md new
        entries #245-#248 (Phase 2 recall-gating/message-threading/
        malformed-request decisions, Phase 3 backfill-idempotency/
        RECALL_DEMO_MARKER/TS-drift-fix decisions, Phase 4 verification-
        technique notes, close-out residual-limitations + Sprint 19
        boundary entry); README.md and docs/design/UX_SPEC.md confirmed
        unchanged (no CEO-facing change landed); FOR_CTO.md synced: §2
        (Current State snapshot updated to Sprint 18), §5.1 (memory_records
        domain table row), §8.3 (MEMORY_RECORDED/MEMORY_RECALLED event
        family notes), §12.14 (new architectural decision entry), §13
        (Sprint-18-specific invariant bullet list), §14.1 (memory module
        critical-files rows + migration head), §14.3 (doc index refresh),
        §18b (new Sprint 18 Handover section), §19 (5 new CTO warnings)
[x] 4.9 residual limitations recorded honestly in DECISIONS.md #248: naive
        keyword/tag substring match (no stemming/fuzzy/semantic), no
        cross-Company memory, no vector recall/RAG, no CEO-facing surface,
        only 6 of 8 originally-sketched categories populated, backfill is
        operator-run not automatic
[x] 4.10 Sprint 19 boundary recorded in DECISIONS.md #248: no Mission Tree
        work, no memory-consuming widget, no workspace_widgets/registry.py
        change, no new workspace route -- anything Sprint 19 does with
        Memory is that sprint's own scoped brief
[x] 4.11 final commit + push -- 4219d37 "docs(sprint18): Phase 4 --
        regression verification and documentation sync"
[x] 4.12 verify clean working tree -- confirmed
[x] 4.13 verify local HEAD == origin/master -- confirmed

CLOSE-OUT NOTES (fill in at Phase 4)

================================================
 SPRINT 19 — OpenRouter provider, observability, release readiness
================================================

PHASE 0 — Baseline verification and architecture decisions
[x] 0.1 verified local HEAD == origin/master (dbb299b, Sprint 18 close);
        working tree clean except untracked docs/prompts/sprint-19.md
[x] 0.2 read sprint-19.md in full (§1-13); read CLAUDE.md, DECISIONS.md
        (through #248), ARCHITECTURE.md, UX_SPEC.md relevant sections
[x] 0.3 read provider_gateway/ (anthropic_provider.py, gateway.py,
        mock_provider.py, __init__.py protocol), model_registry/registry.py,
        core/config.py, core/secrets.py, core/boot_checks.py, main.py,
        workflow_engine/engine.py (_spawn/_run_pipeline/_run_role/
        _run_engineer_tool_loop), scripts/verify_real_llm.py, Makefile,
        projects/service.py::create_project, agent_runtime/service.py::
        hire_employee, planning/service.py::begin_execution -- confirmed
        exact call sequence needed for Phase 3's full Specification-
        lifecycle E2E script and Phase 1's provider/registry/boot-check
        wiring points
[x] 0.4 queried OpenRouter's live catalog directly (curl
        https://openrouter.ai/api/v1/models, filtered on
        pricing.prompt=="0" and pricing.completion=="0" and "tools" in
        supported_parameters) rather than trusting stale training data or
        a WebFetch of the JS-rendered /models page (which only returned
        embedding/audio models -- confirmed the rendered page lazy-loads
        its filtered table client-side). Result: 17 free + tool-capable
        models as of 2026-08-19 (see DECISIONS.md #249)
[x] 0.5 architecture decision recorded: DECISIONS.md #249 -- OpenRouter
        default model registry (openai/gpt-oss-20b:free for all 6 logical
        refs; AVAILABLE_MODELS also offers z-ai/glm-5.2:free,
        nvidia/nemotron-3-super-120b-a12b:free, and the paid
        anthropic/claude-sonnet-4.5 for CEO override / §4.6 release
        evidence); log formatter shape (per §4.9, no new decision needed
        beyond confirming the brief's spec); §4.5 vs §4.6 script-split
        decision (extend verify_real_llm.py for both the two release-
        evidence runs AND the free-tier full-Specification-lifecycle smoke
        test, via a --provider flag plus a --full-lifecycle flag rather
        than a second script, to avoid duplicating throwaway-DB bootstrap
        code)
[x] 0.6 docs/KNOWN_ISSUES.md skeleton drafted (accepted tradeoffs +
        Sprint 15-18 deferrals filled in now; Sprint 19 load-smoke,
        provider-variance, and upgrade-caveat sections left as TODO for
        Phase 3-4 evidence)
[x] 0.7 PROGRESS.txt updated with Sprint 19 live checklist
[x] 0.8 Phase 0 commit + push

PHASE 1 — OpenRouter provider + model registry + boot check
[x] 1.1 apps/api/app/modules/provider_gateway/openrouter_provider.py --
        implements ProviderGateway from scratch (not an AnthropicProvider
        subclass, per sprint-19.md §4.2). Translates (system, messages) ->
        OpenAI messages incl. Anthropic-shaped tool_use/tool_result content
        blocks (needed for the Agent Harness tool loop to work unmodified),
        tools=[{name,description,input_schema}] -> OpenAI function-tool
        shape, and the OpenAI response (content/tool_calls/finish_reason)
        back to CompletionResult/ToolCallData. 401/403 -> CEO-legible
        RuntimeError, same discipline as AnthropicProvider. stream()
        supported for text-only streaming
[x] 1.2 config.py (openrouter added to commander_provider Literal +
        openrouter_api_key field) / secrets.py (_ENV_DEFAULTS entry) /
        boot_checks.py (openrouter branch, same shape as the anthropic
        check) extended
[x] 1.3 model_registry/registry.py: openrouter map (all 6 logical refs,
        not just the 4 sprint-19.md §4.3 names -- reporter-default/
        situation-default included for parity with anthropic, see
        DECISIONS.md #249) -> openai/gpt-oss-20b:free; AVAILABLE_MODELS
        adds 3 free options + paid anthropic/claude-sonnet-4.5;
        PRICE_PER_MILLION_TOKENS entries for all 4
[x] 1.4 gateway.py::build_gateway new openrouter branch -> OpenRouterProvider
[x] 1.5 .env.example (OPENROUTER_API_KEY=), verify_real_llm.py --provider
        {anthropic,openrouter} flag (default anthropic), Makefile
        verify-llm-openrouter target added alongside verify-llm
[x] 1.6 tests: tests/test_openrouter_provider.py (401/403/429/missing-key,
        text + tool-call response translation, malformed tool-call JSON
        tolerance, request message/tool translation incl. tool_use/
        tool_result blocks, streaming) -- 20 new tests; build_gateway
        dispatch tests + openrouter model-resolution/options-for-role
        tests added to test_model_registry.py; boot-check-failure/pass
        tests added to test_boot_checks.py
[x] 1.7 full backend suite green: 532 passed / 6 skipped (512 baseline +
        20 new), zero regressions -- Phase 1 commit + push next

PHASE 2 — Correlation ID + structured logging
[x] 2.1 core/logging.py -- request_id_var/task_id_var/agent_id_var/
        project_id_var contextvars, JSONFormatter (ts/level/logger/msg +
        ambient contextvars + exc traceback + secret-key redaction of
        caller `extra={}` fields only, via _STANDARD_RECORD_KEYS scoping
        -- a deliberate refinement of sprint-19.md §7.3's illustrative
        code, which would otherwise redact/include stock LogRecord
        attributes like pathname/lineno), install_logging()
[x] 2.2 main.py: install_logging() as first line of lifespan(); §7.1's
        exact correlation_id_middleware (fresh UUID per request, never
        trusts incoming X-Request-Id, sets response header), registered
        before CORSMiddleware so it wraps the whole request
[x] 2.3 workflow_engine.py contextvar wiring: _spawn's _runner() wraps
        _run_pipeline in task_id_var.set/reset; _run_role and
        _run_engineer_tool_loop each wrap their existing try/except body
        in an outer try/finally setting/resetting agent_id_var and
        project_id_var (existing CancelledError/Exception handling
        unchanged, just re-indented one level)
[x] 2.4 tests: tests/test_logging.py (formatter ts/level/logger/msg,
        request_id/task_id/agent_id/project_id set->included vs
        not-set->omitted, secret-key redaction exact-match not substring,
        standard LogRecord attrs never redacted, exception traceback,
        install_logging() replaces root handlers -- 9 tests);
        tests/test_correlation_id_middleware.py (valid UUID response
        header, client-supplied X-Request-Id ignored, distinct id per
        request, contextvar reset after request -- 4 tests);
        tests/test_workflow_engine_contextvars.py (document-mission
        _stream_say spy + code-mission run_tool_loop spy confirm ambient
        agent_id_var/project_id_var/task_id_var match the real ids at
        call time; cancelled-pipeline log carries task_id but not
        agent_id/project_id, proving the outer _run_pipeline log sees
        only _spawn's still-open task_id scope -- 3 tests); full backend
        suite green: 553 passed / 6 skipped (532 baseline + 21 new),
        zero regressions -- Phase 2 commit + push next

PHASE 3 — Load smoke + real-LLM E2E + deployment doc
[x] 3.1 scripts/load_smoke.py (4 scenarios) -- all 4 PASS, stable across
        repeated runs. Building it surfaced 2 real concurrent-Mission
        races, fixed in product code (DECISIONS.md #250):
          - engine.py: new DBAgentRuntime._claim_agent bounded poll/retry
            wrapper around the IDLE->ASSIGNED transition, so a busy
            fallback Employee (single-Employee-per-Role founding roster
            + concurrent Missions) is waited out instead of crashing with
            InvalidTransition.
          - agent_runtime/service.py: transition() rewritten as an atomic
            compare-and-swap UPDATE ... WHERE state=:current instead of a
            non-atomic read-then-write, closing a race where 2 concurrent
            pipelines could both validate against the same stale read.
        Also surfaced (and fixed) a load-smoke-script-only deadlock: SSE
        scenario 2 was hanging under httpx.ASGITransport for ANY number
        of concurrent /api/events/stream connections (including 1) --
        root cause is ASGITransport itself, which buffers the whole ASGI
        response before returning, incompatible with the never-
        terminating SSE response. Fixed by having scenario 2 boot a real
        uvicorn.Server on 127.0.0.1:0 instead (uvicorn is already a
        runtime dep, so no new dependency). Two defensive hardenings also
        landed while diagnosing (kept even though neither was the actual
        cause): correlation-ID middleware converted from
        @app.middleware("http")/BaseHTTPMiddleware to plain ASGI in
        main.py; removed a redundant request.is_disconnected() poll in
        realtime/routes.py that raced sse_starlette's own disconnect
        listener for the same one-shot receive() channel.
        Full backend suite re-verified green after all fixes: 553 passed
        / 6 skipped, zero regressions.
[x] 3.2 make verify-llm (Anthropic direct): UNVERIFIED IN THIS ENVIRONMENT --
        no ANTHROPIC_API_KEY configured in this dev environment's .env.
        Recorded honestly in KNOWN_ISSUES.md §7 rather than faked (CLAUDE.md
        §16.7). Needs a funded key supplied by the operator to capture.
[x] 3.3 make verify-llm-openrouter -> anthropic/claude-sonnet-4.5: UNVERIFIED
        IN THIS ENVIRONMENT -- the configured OPENROUTER_API_KEY account has
        no funded credits; request fails immediately with a real
        402 Payment Required (confirms Rule #18 surfaces provider failure
        cleanly, but is not the release-evidence run itself). Recorded in
        KNOWN_ISSUES.md §7. Needs OpenRouter account credits to capture.
[x] 3.4 make verify-llm-openrouter full Specification lifecycle against
        free-tier default (openai/gpt-oss-20b:free) run several times.
        Findings recorded in KNOWN_ISSUES.md §7: provider wiring works;
        free-tier reliability varies turn to turn (malformed tool-call
        JSON, occasional clarification_required, reliably reproduced
        429 rate limiting on consecutive requests); Rule #18 held under
        every real failure observed (clean FAILED state, never a hang or
        raw traceback).
[x] 3.5 docs/DEPLOYMENT.md (new) + .env.production.example (new) written;
        walkthrough dry-run against this repo's own Postgres 16 container:
        `make db-upgrade` clean, plain `uvicorn app.main:app` boot clean,
        boot checks passed, alembic_head == db_revision. Alembic
        round-trip verified against the same real Postgres instance:
        `downgrade -1` then `upgrade head` both completed with no errors,
        final revision confirmed back at head (c2a7e1f4b6d3).
[x] 3.6 commit + push

PHASE 4 — Security audit + docs + close-out
[x] 4.1 full backend suite green (553 passed / 6 skipped, re-verified
        after the logging fix below, 0 regressions); dashboard typecheck
        clean; dashboard production build clean (all routes compiled).
[x] 4.2 independent security audit (dedicated agent) against sprint-19.md
        §2's 7 security requirements. 6/7 PASS outright. 1 real finding:
        apps/api/app/core/logging.py's secret-key redaction matched
        extra={} keys by EXACT string equality against a small blocklist
        (password/token/key/secret/authorization/cookie) -- common
        real-world secret field names (api_key, auth_token,
        password_hash) did not match and would have logged in the clear.
        No live call site does this today, so no active leak, but the
        guard didn't do what it claimed. FIXED: substring match instead
        of exact match (docs/DECISIONS.md #251); tests/test_logging.py
        updated to assert the previously-unredacted variants are now
        redacted. This supersedes the Phase 2 "exact-match, not
        substring" design note -- Rule #7 outranks the earlier
        convenience choice. Full suite re-verified green after the fix.
[x] 4.3 docs/DEPLOYMENT.md, .env.production.example, docs/KNOWN_ISSUES.md
        done in Phase 3 (3.5). CHANGELOG.md: new, v1.1.0 entry.
[x] 4.4 README.md, CLAUDE.md, ARCHITECTURE.md, DECISIONS.md #249-251
        close-out, FOR_CTO.md sync.
[x] 4.5 final commit + push -- Sprint 19 done (NO v1.1.0 tag -- CEO's
        own responsibility per explicit instruction)
[x] 4.6 verify local HEAD == origin/master, working tree clean

CLOSE-OUT NOTES (fill in at Phase 4)

Sprint 19 is done. What shipped:
  - OpenRouterProvider: a from-scratch ProviderGateway implementation
    (not an AnthropicProvider subclass) -- COMMANDER_PROVIDER now accepts
    mock | anthropic | openrouter, same SecretsProvider/boot-check/model-
    override plumbing across all three (DECISIONS.md #249).
  - Structured JSON logging (app/core/logging.py) with per-request
    correlation IDs (plain ASGI middleware, not BaseHTTPMiddleware --
    DECISIONS.md #250) and per-Mission task_id/agent_id/project_id
    contextvars. Secret-shaped field redaction is substring-match, not
    exact-match -- fixed mid-Phase-4 after the independent security audit
    found the exact-match version would leak real field names like
    api_key/auth_token/password_hash (DECISIONS.md #251).
  - scripts/load_smoke.py: 4 scenarios, all PASS, documented as the
    verified operating envelope in docs/KNOWN_ISSUES.md. Building it
    surfaced and fixed two real concurrent-Mission races in
    agent_runtime/workflow_engine (DECISIONS.md #250).
  - docs/DEPLOYMENT.md + .env.production.example (new) -- first
    end-to-end deployment walkthrough, dry-run verified against a real
    Postgres 16 container; Alembic downgrade/upgrade round-trip also
    verified against real Postgres, not just SQLite.
  - docs/KNOWN_ISSUES.md (new), CHANGELOG.md (new) -- consolidated
    tradeoffs, verified envelope, and the v1.1.0 release-note draft.
  - Independent security audit (dedicated subagent, 7-point checklist):
    6/7 PASS, 1 real finding (the redaction bug above), fixed and
    re-verified.

What was validated (honestly), per CLAUDE.md Rule #16.7:
  - "unit test passed": yes -- 553 passed / 6 skipped, backend suite,
    twice this session (after Phase 3 docs, and again after the Phase 4
    logging fix). Zero regressions either run.
  - "typecheck / build passed": yes -- dashboard tsc --noEmit clean;
    dashboard `next build` clean, 19 routes.
  - "API endpoint passed": yes -- OpenRouter free-tier Specification
    lifecycle exercised for real (multiple runs; findings in
    docs/KNOWN_ISSUES.md §7); dry-run deployment boot verified against
    real Postgres.
  - "browser behavior verified": NOT claimed this sprint -- no UI-level
    change shipped in Sprint 19 that required it; CLAUDE.md's browser-
    verification requirement is scoped to UI/E2E work, which this sprint
    was not.
  - "real-LLM E2E passed": PARTIALLY. OpenRouter free-tier models were
    exercised for real and their failure modes (malformed JSON,
    clarification_required, 429) were observed and handled correctly per
    Rule #18. The two specific paid release-evidence runs sprint-19.md
    asked for -- `make verify-llm` (Anthropic direct) and
    `make verify-llm-openrouter --model anthropic/claude-sonnet-4.5`
    (Claude via OpenRouter) -- are UNVERIFIED IN THIS ENVIRONMENT: no
    ANTHROPIC_API_KEY is configured, and the OPENROUTER_API_KEY account
    is unfunded (confirmed via a real, correctly-surfaced 402). This is a
    credentials gap, not a code gap -- the same call path was exercised
    successfully end-to-end via free-tier OpenRouter models. See
    docs/KNOWN_ISSUES.md §7's "release-evidence runs" subsection and
    FOR_CTO.md §18d for the full honest accounting.

What was deliberately NOT done:
  - The v1.1.0 git tag was NOT created. Per the CEO's standing
    instruction, that tag/release commit is the CEO's own call, made
    after personal hands-on browser verification. This close-out commit
    states only that Sprint 19 is done.
  - CLAUDE.md/README.md status lines were deliberately worded to avoid
    declaring "V1.1 released" as a flat fact, even though sprint-19.md
    §4.11 suggested that language -- that declaration is reserved for the
    CEO. See CLAUDE.md's status header and DECISIONS.md's Sprint 19
    close-out entry.

Next steps for whoever picks this up next (CEO or a future sprint):
  - Fund the OPENROUTER_API_KEY account (or configure ANTHROPIC_API_KEY)
    and re-run make verify-llm / make verify-llm-openrouter to close the
    one remaining Definition-of-Done gap.
  - After the CEO's own hands-on browser verification, the CEO tags
    v1.1.0 personally.
