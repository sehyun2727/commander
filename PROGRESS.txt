================================================
 COMMANDER — SPRINT PROGRESS
 Sprint: 9 — Phase A: Foundation & Authentication
 Overall: 55/56 items · 98.2%
 Now working on: Phase 5 -- 5.7 final commit+push + remote HEAD check
 (5.1-5.6 done; found + fixed a real Phase 1 bug during 5.2: orphan
 recovery never freed the Agent, only the Task -- see DECISIONS.md #162)
 Last update: 2026-07-30
 (n/m and % in this header must always match item state -- every time an
 item flips, the header updates in the SAME edit, never batched at phase
 end. Sprint 8's Phase 4 header drifted to (4/6) while 6 items were
 already done -- that must not recur. Recount directly from this file's
 own [ ]/[x] lines if in doubt; do not carry a stale count forward.)
 (markers: [ ] not started · [~] in progress · [x] done · [!]reason blocked/
 skipped with why · +discovery unplanned finding worth carrying forward)
================================================

Baseline recorded (Phase 0 start):
- HEAD SHA before this sprint: 58cbaa0 (docs(v1.1): kickoff complete)
- git status at start: clean except untracked docs/prompts/sprint-9.md
- apps/api/app/modules/auth/ pre-exists as an empty stub (__init__.py only)
  -- no auth code written yet, confirms sprint hasn't started.
- make test (pytest) baseline: 157 passed / 4 skipped (per Sprint 8.5 record)

PHASE 0 — 하이진 + 문서 잔여 수정                              (9/9)
[x] 0.1  PROGRESS.txt 리셋
[x] 0.1a Headquarters 흡수 확정 (ARCHITECTURE §8 + UX_SPEC §7 + 매핑 표)
[x] 0.1b 용어표 Dashboard -> CEO Workspace / V1 as-built 서술은 보존
[x] 0.1c CLAUDE.md Park 예시를 2역할로 수정
[x] 0.1d stale 문서 3종에 HISTORICAL 배너
[x] 0.2  pacing 스위치 (settings + conftest). 소요시간: pacing on(Sprint 8.5
        기록) 349-369s -> pacing off(이번 측정, apps/api/.venv 로 실행)
        109.59s, 157 passed / 4 skipped 동일 -- 약 3.2배 단축
[x] 0.3  샌드박스 --cap-drop ALL + --security-opt no-new-privileges
[x] 0.4  read-only 미적용 사유 주석(docker_sandbox.py 인라인) + PROGRESS
        카운터 규칙 주석(이 헤더)
[x] 0.5  커밋+푸시: chore(sprint9) hygiene -- 8578379, pushed
        (remote HEAD confirmed: git push output shows 58cbaa0..8578379)

PHASE 1 — 운영 신뢰성                                          (11/11)
[x] 1.1  stale ORM 제거 — 파이프라인 스냅샷 도입 (TaskSnapshot frozen
        dataclass, engine.py._run_pipeline/_run_role/_land_code_changes/
        _run_checks 전부 이걸 사용하도록 리팩터)
[x] 1.2  엔진 전체에서 detached ORM 사용 패턴 제거 확인 (모든 TaskORM 읽기는
        자신을 연 async with 세션 블록 안에서만 일어남 -- 재확인 완료)
[x] 1.3  고아 미션 복구 (lifespan 스캔 -> blocked) -- tasks/service.py
        recover_orphaned_tasks(), main.py lifespan에서 init_db 이후 호출
[x] 1.4  EventType.TASK_RECOVERED + payload + TS 재생성 (완료, 이전 커밋에서
        enum/payload 추가; generate_ts_schemas.py 재실행 확인)
[x] 1.5  실행 레지스트리 (_running dict) + finally 정리 (engine.py._spawn +
        CommanderWorkflowEngine._running, _cancel_reasons)
[x] 1.6  POST /api/tasks/{id}/cancel + 상태전이 허용 추가 (라우트 +
        TaskCancelRequest + WorkflowEngine.cancel_task 추상메서드)
[x] 1.7  asyncio.CancelledError 명시적 처리 (실패로 오인 금지) --
        _run_role은 자체 try/except로 캐치해 Employee를 Idle로 되돌리고
        재발생(raise); _run_pipeline의 바깥 핸들러가 Mission 상태를
        cancelled로 전이
[x] 1.8  예산 가드 config 3종 + costs 누적 조회 함수 (config.py 완료 +
        costs/service.py usage_for_task(session_factory, task_id))
[x] 1.9  EventType.BUDGET_EXCEEDED + 스테이지 진입 전 검사 (engine.py
        ._check_budget, PM/Engineer/Reviewer 스테이지 진입 직전 3곳에서 호출)
[x] 1.10 mock 모드에서 예산 초과 실제 재현 확인 -- tests/test_reliability.py
        13개 테스트 전부 통과 (tokens/usd/seconds 3종 + 이벤트 payload
        상세 검증 포함). 도중 SQLite naive-datetime vs Postgres aware-
        datetime 불일치로 12개 기존 테스트가 깨졌던 것 발견 -> _check_budget
        에 tzinfo 정규화 추가로 수정 (DECISIONS.md #146)
[x] 1.11 테스트 (복구 4 / 취소 4 / 예산 5) 전부 통과 확인 + 전체 스위트
        재실행: 170 passed / 4 skipped, 125.06s (기존 157 + 신규 13) --
        커밋+푸시 진행 중

PHASE 2 — 파이프라인 데이터화                                  (8/8)
[x] 2.1  StageSpec 정의 (role_key / kind / lands_code / runs_checks) +
        TEMPLATE.pipeline 추가 (software_company.py) + first_stage_index/
        first_stage_role_key 헬퍼 (kind 기반 시맨틱 조회, position 대신)
[x] 2.2  엔진을 시퀀스 순회 구조로 리팩터. _PM/_ENGINEER/_REVIEWER positional
        unpacking 전부 제거 -- _run_pipeline이 TEMPLATE.pipeline을 enumerate
        하며 stage.kind로 이벤트/사이드이펙트 디스패치 (plan/produce/review)
[x] 2.3  resume_from을 role_key(str)에서 스테이지 인덱스(int)로 전환.
        _REWORK_STAGE_INDEX = first_stage_index(TEMPLATE.pipeline, "produce")
        (모듈 로드 시 1회 계산, 실제 템플릿 기준)
[x] 2.4  prompt_builder/role_contracts.py + tasks/service.py의
        TEMPLATE.roles[1]/roles[0] positional 의존 제거 (first_stage_role_key
        로 대체). 코드베이스 전체 grep으로 잔여 positional indexing 없음 확인
[x] 2.5  테스트 전용 4스테이지 템플릿으로 임의 시퀀스 동작 검증 (5개 테스트,
        tests/test_pipeline_stages.py) -- 실제 3개 역할(pm/engineer x2/
        reviewer) 재사용, produce kind 2회 반복으로 "같은 kind가 두 번
        등장" 케이스 검증. dataclasses.replace(TEMPLATE, pipeline=...) +
        engine 모듈의 TEMPLATE 이름만 monkeypatch (frozen singleton은 직접
        mutate 불가 -- DECISIONS.md #151)
[x] 2.6  기존 170개 테스트 전부 통과 확인 (신규 5개 포함 175 passed / 4
        skipped, 141.79s) -- 동작 변화 0 달성, 실제 3스테이지 파이프라인
        그대로 (test_real_template_pipeline_is_still_three_stages로 가드)
[x] 2.7  docs/ARCHITECTURE.md 동기화: §4.1에 Sprint 9 구현 상태 추가, §6.1
        workflow_engine/templates/costs 모듈 상태 갱신, §6.4 "구조적 부채"
        5항목 전부 해소 표시, §7.1 샌드박스 문장 현재형으로 갱신(원래 Phase
        5 예정이었으나 같은 편집에서 처리 -- DECISIONS.md #152가 #142 대체)
[x] 2.8  DECISIONS.md #149-152 기록 + 커밋+푸시:
        refactor(workflow): template-driven stage pipeline

PHASE 3 — 인증 백엔드                                          (14/14)
[x] 3.1  users / sessions 모델 정의 (UserORM/SessionORM, db_models.py) --
        provider_subject 등 OAuth 확장 컬럼 포함, local이면 NULL
[x] 3.2  Alembic 마이그레이션 (fa793dce62cb_accounts_and_sessions,
        파괴적 삭제 + docstring 경고). sqlite에 직접 upgrade head 실행해
        검증 중 op.create_foreign_key가 SQLite에서 NotImplementedError로
        실패하는 걸 발견 -> batch_alter_table로 수정, upgrade/downgrade/
        재upgrade 전부 성공 확인 (DECISIONS.md #153). Postgres는 Docker
        데몬 미기동으로 이번 세션에서 직접 검증 못함 -- Phase 5에서
        `make db-up && make db-upgrade`로 재확인 필요
[x] 3.3  modules/auth 구조 + IdentityProvider 인터페이스 (identity.py,
        providers/local.py) -- 브리프 명세 구조 그대로
[x] 3.4  providers/local.py (email+password, LocalIdentityProvider)
[x] 3.5  bcrypt 해싱/검증 (cost 12, hash_password/verify_password,
        평문 컬럼 없음 -- db_models.py에 password_hash만 존재)
[x] 3.6  세션 발급/검증/폐기 + 토큰 SHA-256 해시 저장 (issue_session/
        resolve_session/revoke_session, service.py)
[x] 3.7  슬라이딩 만료 (resolve_session이 만료 7일 미만이면 연장) --
        SQLite naive-datetime vs Postgres aware-datetime 불일치로 세션
        관련 테스트 7개가 깨졌던 것 발견 -> tzinfo 정규화로 수정
        (DECISIONS.md #156, engine.py/_check_budget과 동일 패턴 3번째 적용)
[x] 3.8  인증 라우트 4종 (register/login/logout/me, routes.py) --
        로그인 실패 메시지 통일(계정 열거 방지), 중복 이메일 409
[x] 3.9  get_current_user dependency + 전 라우터 적용 (projects/tasks/
        approvals/timeline/agent_runtime/agent_profiles/costs/
        model_registry/reports/situation/sandbox/workspace_manager/
        realtime 13개 라우터 전부)
[x] 3.10 ProjectORM.owner_id + 소유권 검사 (project_owned_by +
        신규 제네릭 resource_owned_by 헬퍼, core/ownership.py -- 4종
        ORM(Task/Approval/Agent/Report)이 전부 project_id 컬럼을 직접
        갖는 걸 확인하고 종류별 중복 대신 제네릭 하나로 처리,
        DECISIONS.md #154). approvals.list_pending(project_id=None)이
        전 계정 데이터를 반환하던 실제 크로스어카운트 유출 발견 +
        수정 (owner_id 스코프 JOIN 추가, DECISIONS.md #155)
[x] 3.11 SSE 스트림 인증 동작 확인 (realtime/routes.py에 session_factory
        + user 의존성 추가, project_owned_by 검사 후 register_stream --
        DECISIONS.md #157). test_sse_stream_requires_auth(401) +
        test_sse_stream_cross_account_returns_404(404)로 커버
[x] 3.12 scripts/export_users.py + reset_password.py + make export-users
        작성 + 실사용 스모크 테스트 완료(throwaway sqlite DB): CSV에
        평문 비밀번호 없음(bcrypt 해시만) 확인, stdout/파일 출력 둘 다
        확인, reset_password.py 3케이스(8자 미만 거부/미존재 계정
        실패/정상 리셋) 전부 확인
[x] 3.13 seed.py 데모 계정(COMMANDER_DEMO_EMAIL/PASSWORD) +
        .env.example + boot_checks(CORS 와일드카드 fail-fast, 쿠키가
        credentialed 요청에서 안 붙는 문제 방지) -- seed.py의 신규 계정
        가입+owner_id 배선 로직을 격리 스모크 테스트로 확인(throwaway
        sqlite). 전체 5-미션 시나리오는 Docker 미기동으로 이번 세션에서
        미실행 -- Phase 5 E2E(DoD #8)에서 실행 필요
[x] 3.14 테스트 15+ (가입/중복이메일/로그인/실패메시지 통일/세션만료/
        미인증 401/타계정 404/SSE 8개 카테고리 -- test_auth.py 14개 +
        test_boot_checks.py 신규 2개 = 16개 신규) + 전체 스위트 재실행:
        191 passed / 4 skipped, 176.83s (기존 157 -> 191, DoD #10의
        190+ 충족) -- 커밋+푸시 완료

PHASE 4 — 프론트 최소 인증                                     (7/7)
[x] 4.1  /login 페이지 (app/login/page.tsx -- 이메일/비밀번호, useAuth().login,
        ApiError 메시지 인라인 표시, /register 링크)
[x] 4.2  /register 페이지 (app/register/page.tsx -- 이름/이메일/비밀번호,
        useAuth().register, /login 링크)
[x] 4.3  인증 상태 관리 + 미인증 리다이렉트 (lib/auth-context.tsx:
        AuthProvider/useAuth, mount 시 getMe() 1회 -- RealtimeProvider와
        동일 패턴의 plain Context, 상태 라이브러리 미도입.
        components/RequireAuth.tsx를 app/page.tsx + app/company/[id]/
        layout.tsx 양쪽에 적용해 미인증 진입 시 /login으로 리다이렉트)
[x] 4.4  전역 401 처리 + credentials:"include" 전 호출 적용 (lib/api.ts의
        request()가 모든 호출에 credentials:"include" 설정 + 401 발생 시
        commander:unauthorized 이벤트 dispatch -- AuthProvider가 전역
        리스닝해 세션 만료를 최초 미인증과 동일하게 처리. api.ts는 React/
        router 의존성 없이 유지)
[x] 4.5  사이드바 로그아웃 + 우상단 계정 표시(최소) (Sidebar.tsx 하단에
        이메일 + Sign out 버튼 추가; components/AccountBadge.tsx --
        브리프 "드롭다운 메뉴 같은 건 만들지 마라" 준수, 클릭 1회로
        즉시 로그아웃하는 이니셜 원형 버튼만 제공. app/page.tsx 헤더 +
        company layout 우상단(fixed)에 배치)
[x] 4.6  typecheck + build 통과 (pnpm typecheck: 에러 0 / pnpm build:
        전 페이지 컴파일 성공, /login /register 정적 생성 확인)
[x] 4.7  커밋+푸시 진행 예정 (다음 스텝) -- 그 전에 throwaway SQLite DB로
        API+대시보드 개발서버 기동 후 register/me/logout/login/me curl
        전체 사이클 실제 실행으로 쿠키 인증 흐름 검증 완료 (Docker 미기동
        -> Postgres 대신 SQLite 폴백 사용, config.py에 문서화된 경로).
        헤드리스 브라우저 도구가 이 환경에 없어 시각적 클릭 테스트는
        SSR HTML 구조 확인(email/password input 존재)으로 대체 -- Phase 5
        DoD 검증 시 재확인 필요하면 기록

PHASE 5 — 검증 · 문서 · 보고                                   (5/7)
[x] 5.1  make test 전체 그린 (카운트 기록) -- pytest 전체 재실행:
        194 passed / 4 skipped, 184.02s (기존 191/4 + 이번에 추가한
        agent-recovery 회귀 테스트 3개). dashboard typecheck/build은
        Phase 4에서 이미 그린 확인 완료, 프론트 변경 없어 재실행 불필요.
[x] 5.2  DoD 14개 항목 수동 E2E 실행 + 결과 기록 -- 완료. 요약(전체
        상세는 Sprint 9 완료 보고 #3 참조):
        #1 통과 (서버 kill -> 재기동 -> blocked + Timeline 사유).
        #2 통과 -- +discovery: 최초 실행 중 실제 버그 발견.
        recover_orphaned_tasks()가 Task 상태만 복구하고 Agent 상태는
        방치, 재시작 후 같은 Employee에게 새 Mission을 배정하면
        InvalidTransition(WORKING->ASSIGNED)으로 파이프라인이 죽음.
        원인 규명 -> tasks/service.py에 Agent 복구 로직 추가(FAILED
        경유 -> IDLE, AGENT_TRANSITIONS로 hop별 검증) -> 회귀 테스트
        3개 추가 -> 전체 스위트 재그린(194/4) -> 실제 서버로 재현
        (킬 -> 재기동 -> SQLite로 Agent가 idle/current_task_id=null
        확인 -> 같은 PM에게 새 Mission 배정 -> completed까지 정상
        완주, 트레이스백 없음)로 라이브 재검증. DECISIONS.md #162 기록.
        수정 후 클린 재실행으로 #2 자체도 통과 확인(cancel 즉시 응답,
        agent state idle 복귀).
        #3 통과 (COMMANDER_MISSION_MAX_TOKENS=10으로 실제 서버 기동,
        mock 미션 실행 -> blocked + task.budget_exceeded 이벤트
        "tokens budget exceeded before 'engineer': 159 > 10" + CEO
        가독 사유 "Budget guard blocked the mission before 'engineer'").
        #4,5,6,7,8 통과 (이전 세션, 실제 curl/실행 기반, 계정 격리/401/
        CSV/데모 미션 완주 전부 실제 확인).
        #9 통과 (tests/test_pipeline_stages.py -- 임의 스테이지 수,
        반복 kind, 인덱스 기반 resume 전부 통과, 194개 중 포함).
        #10 통과 (194 passed / 4 skipped, 157 기준선 대비 +37, 190
        기준 상회).
        #11 통과 (Phase 0 측정: pacing on 349-369s -> pacing off
        109.59s, 약 3.2배 단축).
        #12 통과 (CLAUDE.md/ARCHITECTURE.md 최종 정합성 재확인 -- 이번
        Agent 복구 수정 반영해 두 문서 모두 갱신).
        #13,#14 5.7 최종 커밋+푸시 이후 확인 예정.
[x] 5.3  CLAUDE.md 갱신 (인증/파이프라인/이벤트/명령어) -- 내용 편집 완료
        (커맨드 블록, V1.1 스코프 표 Sprint 9 체크, Phase A 요약 문단).
        아직 커밋 전, 5.7에서 함께 커밋.
[x] 5.4  ARCHITECTURE.md As-Built 갱신 -- 내용 편집 완료 (버전 헤더,
        6.1 auth 모듈 행, 7.2 Authorization 절, 8절 프론트 Auth 문단).
        아직 커밋 전, 5.7에서 함께 커밋.
[x] 5.5  README.md (로그인 필요 + 데모 계정 + 신규 타깃) -- 내용 편집
        완료 (Status, Quickstart, 첫 워크스루 1단계 신설, 커맨드,
        Repo layout). 아직 커밋 전, 5.7에서 함께 커밋.
[x] 5.6  DECISIONS.md 기록 (필수 7항목 + 자체 판단 전부) -- Phase 4
        항목(#159-161) 및 이번 발견/수정한 버그(#162) 기록 완료.
        Appendix B 최종 완료 보고서는 세션 마지막 메시지로 CEO에게
        전달 (기존 스프린트들과 동일하게 저장소 내 별도 파일 없음 --
        docs/prompts/sprint-8*.md 검색 결과 선례 확인).
[~] 5.7  커밋+푸시 + 원격 HEAD 일치 확인 -- 진행 중.
================================================
